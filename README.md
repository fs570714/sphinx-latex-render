# Sphinx Latex Render Extension

allows to render latex inside markdown \(even for the latex build output\) compatible with markdown preview enhanced for vscode
allows to use tikz.. to use together with markdown.
